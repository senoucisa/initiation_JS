alert("js okay");

// Les operateurs arithmétiques

// L'addition
// 1-Déclaration et affectation de plusieurs variables:

var nb1, nb2, resultat;
nb1 = 10;
nb2 = 5;

// L' addition de nb1 & nb2 avec l'opérateur "+"
resultat = nb1+nb2;
console.log(resultat);

// la soustraction de nb1 & nb2 avec l'opérateur " -"
resultat = nb1-nb2;
console.log(resultat);

// La division de nb1& nb2 avec l'opérateur "/"
resultat = nb1/ nb2;
console.log(resultat);

// La multiplications de nb1 & nb2 avec l'opérateur "#"
resultat = nb1*nb2;
console.log(resultat);

// Le modulo correspondau reste de la division
// Le modulo de nb1 & nb2 avec l'opérateur "%"

resultat = nb1 % nb2;
console.log(resultat);



    // Les écritures simplifiées

    // Addition

// je crée ma variable nb1 et je lui affecte une valeur de 15(avec le symbole =)
    nb1 = 15;
    // première variante:
    // pour ajouter 5 à am valeur de 15,je réaffecte à la variable une nouvelle instruction :
    // je lui que ma variable nb1 sera maintenant le résultat de ma valeur de base (donc15)auquel je lui rajoute 5:
    // ma variable  la valeur de reference

    // nb1=                nb1          +   5;
    // ici on dit que le resultat final de nb1 est la somme de notre variable+5.
    
    
   

    // Ici on dit que le resultat final de nb1 est la somme de notre variable +5
    // nb1 = nb1 + 5;
    // console.log(nb1);
    // deuxième variante
    // ce qui est équivalent à écrire nb1=nb1+5
    nb1+= 5;
    console.log(nb1);

    // Je peux proceder de la même maniére pour tous les autres opérateurs arithématiques:
    "+", "-", "*", "/" , "%".
    

 